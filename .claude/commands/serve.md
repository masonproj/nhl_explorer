Start the NHL Explorer development server:

```bash
uvicorn main:app --reload
```

The app will be available at http://localhost:8000. The server reloads automatically
when `main.py` or template files change.
